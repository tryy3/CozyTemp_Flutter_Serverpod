### Input data for creating a calibrated temperature reading
class: CalibrationInput
serverOnly: true

fields:
    ## The UUID of the raw data to calibrate
    rawDataId: String # Required
    
    ## The calibrated temperature value
    temperature: double # Required

